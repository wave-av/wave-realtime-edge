{
  "name": "wave-realtime-edge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "3.2.3",
    "wrangler": "^4.50.0"
  }
}
