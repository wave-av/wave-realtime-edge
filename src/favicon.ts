// Realtime favicon — the shared WAVE mark recolored flat to this spoke's accent (single-sourced).
import { fillFavicon } from "@wave-av/spoke-chassis";
import { ACCENT_HEX } from "./tokens.css";
export const FAVICON_SVG = fillFavicon(ACCENT_HEX);
